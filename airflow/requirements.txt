# DB drivers
psycopg[binary]==3.1.18
psycopg2-binary==2.9.9

# Provider postgres (optionnel de pinner)
apache-airflow-providers-postgres

# Data libs (laisse Airflow constraints guider; mais OK de fixer si tu veux)
pandas==2.1.4
numpy>=1.26,<3
pyarrow==16.1.0
polars==0.20.16

# Config (read by src/core/config.py -- the DAGs import the pipeline code, so
# the worker image needs this too, not just the API image).
pydantic-settings==2.2.1

# Vendor + AWS
# 0.2.66: 0.2.37 broke against Yahoo's cookie/crumb requirements (empty bodies).
yfinance==0.2.66
requests==2.31.0
boto3==1.34.69
s3fs

# Utils
python-dotenv==1.0.1
pendulum==3.0.0